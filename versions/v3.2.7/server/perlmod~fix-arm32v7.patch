diff --git a/perlmod-bin/debian/rules b/perlmod-bin/debian/rules
index 955dd78..8b6f6ed 100755
--- a/perlmod-bin/debian/rules
+++ b/perlmod-bin/debian/rules
@@ -9,5 +9,17 @@
 # Uncomment this to turn on verbose mode.
 #export DH_VERBOSE=1
 
+ARCH:= $(shell dpkg-architecture -qDEB_BUILD_ARCH)
+
 %:
 	dh $@
+
+ifeq ($(ARCH), armhf)
+
+override_dh_perl:
+	# do nothing
+
+override_dh_auto_install:
+	dh_auto_install
+	sed -i 's|^#!.*/usr/bin/perl|#!/usr/bin/dh_perl|' debian/perlmod-bin/usr/lib/perlmod/genpackage.pl
+endif
